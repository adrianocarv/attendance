# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

# Specify the DBMS
spring.jpa.database = MYSQL

# Connection url for the database "netgloo_blog"
#spring.datasource.url = jdbc:mysql://localhost:3306/attendance
spring.datasource.url = jdbc:mysql://192.241.180.208:3306/attendance
#spring.datasource.url = jdbc:mysql://us-cdbr-iron-east-04.cleardb.net/heroku_3af9be931e58320?reconnect=true

# Username and password
spring.datasource.username = bumerangue
spring.datasource.password = maria36912
#spring.datasource.username = b1842e64c3b8d5
#spring.datasource.password = a3a6bfcc

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# ===============================
# = DROPBOX
# ===============================
dropbox.accessToken = FSRJVa8x0EIAAAAAAAE_Z919a-Tw2oftUJQFedto2vZ1phmFsOWyRoj3FN_CVdAj
dropbox.rootPath = /Apps/Attendance